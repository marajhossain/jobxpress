{% extends 'base.html.twig' %}

{% block title %}Create New Job {% endblock %}

{% block body %}

<div class="col-lg-12">
    <h1 class="page-header">Create new JobPost</h1>
</div>
    
    {{ form_start(form, {'method': 'POST', 'action': path("new_job") }) }}
        {{ include('job_post/_form.html.twig') }} 
    {{ form_end(form) }}

    

    <a href="{{ path('job_post_index') }}" class="text-danger"> <i class="fa fa-arrow-left"></i> back to list</a>
    
{% endblock %}

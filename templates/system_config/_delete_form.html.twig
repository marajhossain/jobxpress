<form method="post" action="{{ path('system_config_delete', {'id': system_config.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ system_config.id) }}">
    <button class="btn">Delete</button>
</form>
